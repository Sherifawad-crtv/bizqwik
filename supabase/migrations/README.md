# Migrations

SQL migrations applied to the Supabase project (`aylhnxniqrihpvrllpgz`), tracked
here for reproducibility. Earlier migrations (pre-client-app) were applied
directly and are recorded in the project's remote migration history; from the
client-app work onward the SQL is committed here as well, newest suffix highest.

Files are the exact statements applied (via the Supabase MCP `apply_migration`),
named `YYYYMMDD_NN_<name>.sql`.
